// Aaron Palomin
// I'm going to define two modules: `http_client` and `website_status`. These are modules that carry out a function 
// whereby they organize relevant function into distinct namespaces. This makes for easier maintenance and understanding of the code.
pub mod http_client;
pub mod website_status;
